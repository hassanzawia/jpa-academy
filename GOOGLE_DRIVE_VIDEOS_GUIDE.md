# دليل استضافة الفيديوهات على Google Drive (خطوة بخطوة)
## منصة الجودة للفارمسي أكاديمي (Al-Jawda Pharmacy Academy)

> بدلاً من رفع الفيديوهات مباشرة إلى GitHub (محدودة بـ 100 ميجابايت لكل ملف ومساحة استضافة صغيرة)، نستخدم **Google Drive** كمكان تخزين مجاني للفيديوهات (15 جيجابايت مجاناً)، ونعرضها داخل الموقع عبر تقنية **iframe /preview** — وهي الطريقة الرسمية الوحيدة المضمونة للعمل مع أي حجم ملف بدون مشاكل CORS أو حظر من جوجل.<cite>turn43search48</cite><cite>turn43search49</cite>

---

## 🎯 لماذا هذه الطريقة تحديداً؟

| الطريقة | تعمل؟ | لماذا |
|---|---|---|
| رابط `/view` مباشر في `<video src="">` | ❌ | Google Drive يرفض تحميل الفيديو الخام مباشرة بهذا الشكل |
| `uc?export=download&id=...` في `<video>` | ⚠️ محدودة | تعمل فقط للملفات أقل من 100 ميجابايت، وتنكسر عند محاولة التقديم (Seek) في الفيديو<cite>turn43search50</cite> |
| **`/file/d/ID/preview` داخل `<iframe>`** | ✅ **موصى بها** | تعمل مع أي حجم ملف، تحافظ على أدوات تحكم المشغّل، ولا تحتاج أي إعداد إضافي<cite>turn43search49</cite><cite>turn43search51</cite> |

هذه هي الطريقة المُستخدمة بالفعل في ملف `course-player.html` ضمن مشروعك.

---

## 📤 الخطوة 1: رفع الفيديو إلى Google Drive

1. اذهب إلى https://drive.google.com وسجّل الدخول بحساب Google الخاص بك.
2. اضغط الزر **"+ New"** (أعلى يسار الصفحة) → **"File upload"**.
3. اختر ملف الفيديو من جهازك (يُفضّل صيغة **MP4** لأفضل توافق).
4. انتظر حتى يكتمل الرفع (حسب حجم الملف وسرعة الإنترنت).

---

## 🔓 الخطوة 2: ضبط صلاحيات المشاركة (خطوة حرجة)

1. بعد الرفع، انقر بزر الفأرة **الأيمن** على الفيديو داخل Drive.
2. اختر **"Share"** (مشاركة).
3. في النافذة التي تظهر، ابحث عن قسم **"General access"**.
4. غيّر الإعداد من "Restricted" إلى:
   ```
   Anyone with the link
   ```
5. تأكد أن الدور المحدد بجانبه هو **"Viewer"** (مُشاهد فقط، وليس محرر).
6. اضغط **"Done"**.

> ⚠️ **هذه الخطوة ضرورية جداً** — بدونها، سيظهر الفيديو كصفحة رمادية فارغة لأي شخص غير مسجّل بحسابك بالتحديد.<cite>turn43search46</cite>

---

## 🔗 الخطوة 3: نسخ رابط المشاركة واستخراج معرّف الملف (File ID)

1. بعد ضبط المشاركة، اضغط **"Copy link"** في نفس النافذة.
2. سيكون الرابط بالشكل التالي:
   ```
   https://drive.google.com/file/d/1AbCdEfGhIJKLmnopQRSTuvwxYZ123456/view?usp=sharing
   ```
3. **معرّف الملف (File ID)** هو الجزء الطويل بين `/d/` و `/view`:
   ```
   1AbCdEfGhIJKLmnopQRSTuvwxYZ123456
   ```
4. انسخ هذا الجزء فقط — هذا ما ستحتاجه في الخطوة التالية.

---

## ✏️ الخطوة 4: إضافة الفيديو إلى موقعك

مشروعك يحتوي ملفاً مخصصاً لهذا الغرض بالضبط:
```
public/js/courses-data.js
```

افتحه، وستجد بنية كل دورة وفصولها بهذا الشكل:

```javascript
{
  id: "ch1-intro",
  title: "الفصل 1: مقدمة في الصرف الآمن",
  driveId: "YOUR_DRIVE_ID_1",   // ← استبدل هذا بمعرّف الملف الذي نسخته
  durationMinutes: 25,
  summaryPdf: "assets/ch1-summary.pdf"
}
```

**استبدل** `"YOUR_DRIVE_ID_1"` بمعرّف الملف الفعلي الذي نسخته، مثلاً:

```javascript
driveId: "1AbCdEfGhIJKLmnopQRSTuvwxYZ123456",
```

كرّر هذا لكل فصل من فصول كل دورة، مع كل معرّف فيديو خاص به.

---

## 🧩 كيف يعمل هذا تقنياً داخل الموقع؟

في ملف `courses-data.js` توجد دالة جاهزة:

```javascript
function buildDriveEmbedUrl(driveId) {
  return `https://drive.google.com/file/d/${driveId}/preview`;
}
```

وفي `course-player.html`، عند اختيار أي فصل، يُبنى رابط `iframe` تلقائياً:

```javascript
document.getElementById("video-frame").src = buildDriveEmbedUrl(ch.driveId);
```

الحاوية نفسها في CSS (`gdrive-video-wrap`) مصممة لتكون متجاوبة بنسبة عرض **16:9** على كل أحجام الشاشات، مع أدوات تحكم Google Drive الأصلية (تشغيل، تقديم، ملء الشاشة).

---

## 🚀 الخطوة 5: رفع التعديل إلى GitHub

بعد تحديث `courses-data.js` بكل معرّفات الفيديوهات الفعلية:

```powershell
cd "C:\Users\HP\OneDrive - Hatif Libya\Desktop\pharmacademy"
git add public/js/courses-data.js
git commit -m "Add Google Drive video links for course chapters"
git push
```

سينشر تلقائياً خلال دقيقة عبر GitHub Actions (كما هو مُعد مسبقاً في مشروعك).

---

## ➕ إضافة دورة أو فصل جديد بالكامل

لإضافة **دورة جديدة تماماً**، أضف كائناً جديداً داخل مصفوفة `COURSES` في `courses-data.js`:

```javascript
{
  id: "new-course-id",              // معرّف فريد بالإنجليزية بدون مسافات
  category: "medicine",              // أحد: medicine, languages, curricula, crafts
  title: "عنوان الدورة الجديدة",
  instructor: "اسم المدرّس",
  instructorTitle: "التخصص",
  thumbnail: "assets/course-new.jpg",
  price: 80,
  currency: "د.ل",
  description: "وصف مختصر للدورة",
  chapters: [
    {
      id: "ch1",
      title: "الفصل 1: العنوان",
      driveId: "معرّف_الفيديو_من_Google_Drive",
      durationMinutes: 30,
      summaryPdf: null   // أو مسار ملف PDF إن وُجد
    }
  ]
}
```

سيظهر تلقائياً في صفحة `courses.html` ضمن التصنيف المحدد، بدون أي حاجة لتعديل أي ملف آخر.

---

## 🛠️ حل المشاكل الشائعة

| # | المشكلة | السبب | الحل |
|---|---|---|---|
| 1 | الفيديو يظهر كصفحة رمادية فارغة | لم تُضبط المشاركة على "Anyone with the link" | ارجع للخطوة 2 وتأكد من إعداد المشاركة |
| 2 | رسالة "This content is currently unavailable" | الفيديو محذوف أو تم تغيير صلاحياته لاحقاً | تحقق من حالة الملف داخل Google Drive نفسه |
| 3 | الفيديو يعمل لكن بجودة منخفضة تلقائياً | Google Drive يُكيّف الجودة حسب سرعة الإنترنت | طبيعي، يمكن للمستخدم تغيير الجودة يدوياً من إعدادات المشغّل (⚙️) |
| 4 | تعذّر تشغيل تلقائي (Autoplay) | متصفحات حديثة تمنع التشغيل التلقائي بالصوت افتراضياً | طبيعي وليس خطأ في الموقع؛ المستخدم يضغط تشغيل يدوياً |
| 5 | الفيديو يعمل على الحاسوب لكن لا يظهر على الهاتف | نادراً ما يحدث بسبب حظر iframe من بعض متصفحات الهواتف القديمة جداً | جرّب متصفح Chrome/Safari محدّث |

---

## 💰 حدود Google Drive المجانية

| المورد | الحد المجاني |
|---|---|
| مساحة التخزين | 15 جيجابايت (تُشارك مع Gmail وSuite بالكامل) |
| عدد المشاهدات/التنزيلات اليومية | محدودة بحصة عادلة غير معلنة رسمياً، لكنها كافية جداً لعدد طلاب متوسط (مئات المشاهدات يومياً) |
| حجم الملف الواحد | لا حد أقصى معلن ضمن المساحة المتاحة |

إن توسّع عدد طلابك بشكل كبير جداً مستقبلاً (آلاف المشاهدات يومياً)، فكّر حينها في الانتقال لخدمة استضافة فيديو متخصصة مثل **Bunny.net** أو **Cloudflare Stream** (خدمات مدفوعة لكن رخيصة جداً ومصممة للبث بكميات كبيرة).

---

**« معايير الجودة في التعليم الصيدلي.. واحترافية الممارسة الإكلينيكية »**
