# إصلاح: صفحة courses.html بيضاء على بعض شبكات الجوّال

## 🐛 وصف المشكلة
`login.html` تعمل بنجاح على الهاتف، لكن `courses.html` تظهر بيضاء تماماً على شبكة 4G معيّنة، رغم أن أداة التشخيص أثبتت أن Firebase (Auth + Firestore) تعمل بنجاح 100% على نفس الهاتف.

## 🔍 السبب الجذري
`courses.html` (خلافاً لـ `login.html`) كانت تُحمّل مكتبتين إضافيتين ضروريتين فقط لنظام الشهادات:
```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
```

هاتان المكتبتان تُستضافان على **cdnjs.cloudflare.com** — وهو نطاق مختلف تماماً عن نطاقات Google (`gstatic.com`, `firestore.googleapis.com`) التي تعمل بنجاح. بعض شبكات الجوّال (خصوصاً في بعض مناطق ليبيا) تحظر أو تُبطئ الوصول لـ Cloudflare CDN تحديداً، بينما تسمح بمرور نطاقات Google بشكل طبيعي.

بما أن هذه السكربتات كانت `<script>` عادية (وليست async/defer)، **المتصفح ينتظرها لتكتمل قبل متابعة تنفيذ باقي الصفحة** — فإن عَلِقت (Hang) بسبب حظر الشبكة، تبقى الصفحة بيضاء إلى الأبد، حتى لو كان كل شيء آخر (Firebase، الكود، البيانات) سليماً 100%.

---

## ✅ الإصلاح المُطبّق: تحميل كسول (Lazy Loading)

### الفكرة
لم نعد نُحمّل `html2canvas` و`jsPDF` في `<head>`/`<body>` إطلاقاً عند فتح الصفحة. بدلاً من ذلك:
1. الصفحة تُحمَّل وتعمل بشكل طبيعي فوراً (الدورات، المحفظة، كل شيء) **بدون انتظار** هاتين المكتبتين.
2. فقط **عند الضغط الفعلي على زر "🎓 الشهادة"**، يبدأ تحميلهما ديناميكياً في الخلفية.
3. إن نجح التحميل (خلال 8 ثوانٍ كحد أقصى لكل مكتبة) → تُنشأ الشهادة كالمعتاد.
4. إن فشل التحميل (شبكة تحظر الوصول، أو انقطاع اتصال) → **رسالة خطأ واضحة** تظهر للمستخدم تحديداً، بدلاً من تعطّل الصفحة بالكامل:
   > ⚠️ تعذّر تحميل أدوات إنشاء الشهادة (قد تكون شبكة الإنترنت الحالية تحظر الوصول لخدمة cdnjs.cloudflare.com). الرجاء المحاولة عبر شبكة WiFi...

---

## 📦 الملفات المُحدَّثة

| الملف | التغيير |
|---|---|
| `public/js/certificates.js` | 🔄 إعادة كتابة كاملة — تحميل كسول + معالجة فشل صريحة + مهلة زمنية وقائية |
| `public/courses.html` | 🔄 إزالة سكربتي `cdnjs.cloudflare.com` الثابتين من الصفحة |

> ✅ **لا حاجة لأي تعديل على `course-player.html`** — لم يكن يحمّل هاتين المكتبتين أصلاً.

---

## 🚀 خطوات النسخ والرفع

```powershell
cd "C:\Users\HP\OneDrive - Hatif Libya\Desktop\pharmacademy"
```

انسخ الملفين من هذه الحزمة (استبدال):
```
public/js/certificates.js
public/courses.html
```

```powershell
git add .
git commit -m "Fix blank page on mobile by lazy-loading certificate PDF libraries"
git push
```

---

## 🧪 خطوات الاختبار

### اختبار 1: تحقق أن courses.html تعمل الآن على شبكة الجوّال المشكلة
1. انتظر دقيقة لاكتمال النشر التلقائي.
2. على الهاتف (بنفس شبكة 4G التي كانت تفشل سابقاً)، امسح الكاش مرة أخيرة (Clear browsing data → All time).
3. افتح `https://jawda-edu.github.io/courses.html`.
4. **يجب أن تظهر الصفحة بمحتواها الكامل** (رصيد المحفظة، بطاقات الدورات) بدون أي بياض.

### اختبار 2: تحقق أن ميزة الشهادات لا تزال تعمل (على WiFi أو شبكة لا تحظر Cloudflare)
1. اذهب لدورة مكتملة الشروط (مثل OTC لحساب `dr_sarah` سابقاً).
2. اضغط **"🎓 الشهادة"**.
3. يجب أن تظهر رسالة "⏳ جارٍ تجهيز الشهادة..." لثانية أو ثانيتين، ثم يُنزَّل ملف PDF كالمعتاد.

### اختبار 3 (تشخيصي اختياري): تأكيد سلوك الفشل الآمن
إن كنت لا تزال على الشبكة التي تحظر Cloudflare، جرّب الضغط على "🎓 الشهادة" فعلياً (إن توفرت لأي دورة) — يجب أن تظهر رسالة الخطأ الواضحة بدلاً من تجمّد الصفحة، وتبقى بقية الصفحة قابلة للاستخدام بشكل طبيعي تماماً.

---

## 💡 ملاحظة مستقبلية (اختيارية)
إن استمرت مشكلة حظر Cloudflare للمستخدمين على شبكات معيّنة حتى مع التحميل الكسول، يمكن مستقبلاً استضافة نسختين محليتين من `html2canvas.min.js` و`jspdf.umd.min.js` مباشرة داخل مجلد `public/js/vendor/` ورفعهما إلى GitHub بدلاً من الاعتماد على أي CDN خارجي إطلاقاً — هذا يضمن عملهما دائماً بغض النظر عن قيود الشبكة.

---

**« معايير الجودة في التعليم الصيدلي.. واحترافية الممارسة الإكلينيكية »**
