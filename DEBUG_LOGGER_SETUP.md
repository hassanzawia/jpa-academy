# لوحة تشخيص ثابتة على الشاشة (حل مشكلة "الصفحة تختفي بسرعة")

## 🎯 لماذا هذا الحل الآن؟

بعد عدة محاولات إصلاح (Service Worker، تحميل كسول للمكتبات، إصلاح Race Condition)، ما زالت المشكلة مستمرة، ولا نملك رؤية مباشرة لما يحدث فعلياً على هاتفك. بدلاً من الاستمرار بالتخمين، هذه الأداة ستُظهر لنا **الحقيقة الدقيقة**: كل خطوة تُنفَّذ، وأي خطأ حقيقي يحدث، مباشرة على شاشة هاتفك، **بشكل ثابت لا يختفي** حتى لو أعيد توجيه الصفحة عدة مرات متتالية.

---

## 🧠 كيف تعمل هذه الأداة؟

### 1. شريط أسود ثابت أسفل الشاشة
بمجرد تحميل `login.html` أو `courses.html`، سيظهر شريط تشخيص أسود بخط أخضر أسفل الشاشة يعرض كل خطوة تحدث لحظياً.

### 2. السجل يُحفظ في `localStorage` (يبقى بين الصفحات)
حتى لو انتقلت الصفحة تلقائياً من `login.html` إلى `courses.html` (أو العكس) بسرعة، **السجل نفسه لا يُمسح** — يستمر بالتراكم عبر الصفحات المتتالية.

### 3. التقاط تلقائي لأي خطأ JavaScript حقيقي
أي خطأ غير متوقع (`window.onerror`) أو أي طلب Firebase فاشل (`unhandledrejection`) يُسجَّل تلقائياً بخط أحمر 🔴 مع تفاصيل دقيقة (رسالة الخطأ، اسم الملف، رقم السطر).

### 4. زر "📋 Copy log" — الحل النهائي لمشكلة "لا أستطيع التقاط صورة"
بدلاً من محاولة التقاط لقطة شاشة لشيء يختفي بسرعة، فقط اضغط هذا الزر لنسخ **كامل السجل كنص**، ثم الصقه مباشرة في رسالتك لي.

---

## 📦 الملفات الجديدة/المُحدَّثة

| الملف | الحالة |
|---|---|
| `public/js/debug-logger.js` | 🆕 جديد بالكامل |
| `public/js/auth.js` | 🔄 محدّث — إضافة `dlog()` في كل خطوة (تسجيل الدخول، إنشاء الملف الشخصي، إعادة التوجيه) |
| `public/js/session-guard.js` | 🔄 محدّث — إضافة `dlog()` في منطق الجلسة الواحدة |
| `public/login.html` | 🔄 محدّث — تحميل `debug-logger.js` أولاً + نقاط تسجيل إضافية |
| `public/courses.html` | 🔄 محدّث — تحميل `debug-logger.js` أولاً + نقاط تسجيل إضافية |

> ملاحظة: لم أُحدّث `course-player.html` و`admin.html` في هذه الحزمة تحديداً، لأن التركيز الآن على تشخيص مشكلة `login.html`/`courses.html`. يمكن إضافة نفس الأداة لهما لاحقاً بنفس الطريقة إن لزم الأمر.

---

## 🚀 خطوات النسخ والرفع

```powershell
cd "C:\Users\HP\OneDrive - Hatif Libya\Desktop\pharmacademy"
```
انسخ الملفات الخمسة من هذه الحزمة (استبدال/إضافة).

```powershell
git add .
git commit -m "Add persistent on-screen debug logger to diagnose mobile issue"
git push
```

---

## 🧪 خطوات الاختبار على هاتفك (هذه المرة ستُرسل لي دليلاً حقيقياً)

### الخطوة 1: انتظر اكتمال النشر
تحقق من: `https://github.com/jawda-edu/jawda-edu.github.io/actions` (✅ خضراء)

### الخطوة 2: امسح كاش المتصفح على الهاتف مرة أخيرة فقط (لضمان تحميل السكربت الجديد)
Chrome → Settings → Privacy → Clear browsing data → All time → Clear

### الخطوة 3: افتح صفحة تسجيل الدخول
```
https://jawda-edu.github.io/login.html
```

### الخطوة 4: راقب الشريط الأسود أسفل الشاشة
يجب أن يبدأ بعرض أسطر مثل:
```
[..] (login.html) === debug-logger.js loaded ===
[..] (login.html) DOMContentLoaded fired
[..] (login.html) login.html: main script started
[..] (login.html) requireAuth() attached, waiting...
```

### الخطوة 5: اضغط على أي حساب تجريبي (مثل dr_sarah)
راقب الأسطر الجديدة التي تُضاف تباعاً.

### الخطوة 6: بغض النظر عما يحدث بعدها (نجاح، فشل، اختفاء، إعادة توجيه)
**اضغط فوراً على زر "📋 Copy log"** الظاهر في الشريط الأسود (حتى لو انتقلت الصفحة، الشريط سيظهر من جديد في الصفحة التالية مع نفس السجل المتراكم — اضغط النسخ من هناك).

### الخطوة 7: الصق السجل المنسوخ مباشرة في رسالتك لي
هذا النص الكامل سيُظهر لي **بالضبط** أين تتوقف العملية أو أي خطأ حقيقي يحدث، بغض النظر عن مدى سرعة اختفاء الصفحة.

---

## 💡 ملاحظة إضافية: إن أردت رؤية السجل يدوياً بدون النسخ
اضغط ببساطة داخل الشريط الأسود واسحب لأعلى قليلاً لرؤية كل الأسطر (فهو قابل للتمرير `overflow-y: auto`)، أو اضغط زر "✖ Hide" لإخفائه مؤقتاً إن أردت رؤية باقي الصفحة بوضوح.

---

**« معايير الجودة في التعليم الصيدلي.. واحترافية الممارسة الإكلينيكية »**
