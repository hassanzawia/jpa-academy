# تفعيل زر الخروج + تقييد الدخول لجهاز واحد فقط — دليل التركيب

## 📦 الملفات الجديدة/المُحدَّثة في هذه الحزمة

| الملف | الحالة |
|---|---|
| `public/js/session-guard.js` | 🆕 جديد — منطق إنشاء ومراقبة الجلسة النشطة الوحيدة |
| `public/js/auth.js` | 🔄 محدّث — تسجيل الدخول ينشئ جلسة جديدة، تسجيل الخروج يمسحها |
| `public/login.html` | 🔄 محدّث — تحميل `session-guard.js` + ملاحظة تنبيهية للمستخدم |
| `public/courses.html` | 🔄 محدّث — **إضافة زر "🚪 تسجيل الخروج" المفقود** + تفعيل حراسة الجلسة |
| `public/course-player.html` | 🔄 محدّث — تفعيل حراسة الجلسة (زر الخروج كان موجوداً بالفعل) |
| `public/admin.html` | 🔄 محدّث — تفعيل حراسة الجلسة (تشمل حساب المدير أيضاً) |

> ✅ **لا حاجة لأي تعديل على `firestore.rules`** — الحقل الجديد `activeSessionId` يُكتب على مستند المستخدم نفسه، وهذا مسموح بالفعل بالقاعدة الحالية `allow read, update: if request.auth.uid == userId`.

---

## 🧠 كيف تعمل الميزة تقنياً؟

### 1. عند تسجيل الدخول
- يُولَّد **معرّف جلسة عشوائي فريد** (Session ID)، مثل: `k3j2h1-lm9p8q`
- يُحفظ في مكانين:
  - **محلياً** في هذا المتصفح تحديداً (`localStorage`)
  - **في Firestore** ضمن مستند المستخدم (`activeSessionId`)

### 2. أثناء استخدام المنصة
كل صفحة محمية (`courses.html`, `course-player.html`, `admin.html`) تستمع **لحظياً** (`onSnapshot`) لأي تغيّر في `activeSessionId`.

### 3. عند تسجيل الدخول من جهاز آخر بنفس الحساب
- الجهاز الجديد يكتب معرّف جلسة **جديد** في Firestore، فيُصبح الجهاز القديم "قديماً" فوراً.
- الجهاز القديم (إن كان مفتوحاً على أي صفحة) يكتشف عدم التطابق **خلال أجزاء من الثانية** بفضل المزامنة الفورية، ويُظهر تنبيهاً:
  > ⚠️ تم تسجيل الدخول إلى هذا الحساب من جهاز آخر. سيتم تسجيل خروجك من هذا الجهاز الآن.
- ثم يُسجَّل خروجه تلقائياً ويُعاد توجيهه إلى `login.html`.

### 4. ملاحظة مهمة: تطبَّق الميزة على **كل** الحسابات
بما أن التدريب يشمل عدة حسابات تجريبية (وربما حساب المدير أيضاً)، الميزة مفعّلة بشكل موحّد على الجميع. هذا يعني:
- إن سجّل شخصان الدخول بنفس بيانات `dr_sarah` من جهازين مختلفين، سيُطرد الجهاز الأول تلقائياً عند دخول الثاني.
- هذا **مثالي لمنع مشاركة الحسابات أثناء التدريب أو الامتحانات**.

---

## 🚀 خطوات النسخ والرفع

### الخطوة 1: انسخ الملفات إلى مشروعك
```
public/js/session-guard.js     (جديد)
public/js/auth.js              (استبدال)
public/login.html              (استبدال)
public/courses.html            (استبدال)
public/course-player.html      (استبدال)
public/admin.html              (استبدال)
```

### الخطوة 2: ارفع التحديث (لا حاجة لأي خطوة في Firebase Console هذه المرة)
```powershell
cd "C:\Users\HP\OneDrive - Hatif Libya\Desktop\pharmacademy"
git add .
git commit -m "Add logout button and single-device session enforcement"
git push
```

---

## 🧪 خطوات الاختبار الكامل

### اختبار 1: التأكد من ظهور زر الخروج في كل الصفحات
1. سجّل الدخول بأي حساب.
2. تحقق من ظهور زر **"🚪 تسجيل الخروج"** في: `courses.html`، `course-player.html`، `admin.html` (لحساب المدير).

### اختبار 2: تقييد الدخول لجهاز واحد (الاختبار الأهم)
1. افتح **متصفحين مختلفين** (مثلاً Chrome عادي + Chrome بوضع التصفح المتخفي Incognito)، أو جهازين فعليين (حاسوب + هاتف).
2. في **المتصفح الأول**: سجّل الدخول بحساب `nour_intern` وابقَ في صفحة `courses.html`.
3. في **المتصفح الثاني**: سجّل الدخول بنفس حساب `nour_intern`.
4. **ارجع للمتصفح الأول خلال ثوانٍ** (بدون تحديث الصفحة يدوياً) — يجب أن يظهر تلقائياً تنبيه:
   > ⚠️ تم تسجيل الدخول إلى هذا الحساب من جهاز آخر...
5. بعد الضغط على "OK"، يُعاد توجيه المتصفح الأول تلقائياً إلى `login.html`.
6. المتصفح الثاني يستمر في العمل بشكل طبيعي تماماً (هو الجلسة "النشطة" الوحيدة الآن).

### اختبار 3: تسجيل الخروج يمسح الجلسة بشكل نظيف
1. سجّل الدخول بأي حساب.
2. اضغط **"🚪 تسجيل الخروج"**.
3. سجّل الدخول بنفس الحساب مرة أخرى من نفس الجهاز — يجب أن يعمل بشكل طبيعي تماماً بدون أي تعارض.

---

## 🛠️ حل المشاكل الشائعة

| المشكلة | السبب | الحل |
|---|---|---|
| تم تسجيل خروجي فوراً بعد الدخول مباشرة | ربما فتحت تبويبين لنفس الحساب في نفس المتصفح سابقاً | امسح localStorage (F12 → Application → Local Storage → Clear) وسجّل الدخول من جديد |
| التنبيه لا يظهر فوراً عند دخول جهاز آخر | تأخر بسيط في الشبكة، أو الصفحة كانت في الخلفية (تبويب غير نشط) | التأثير يحدث فوراً عملياً؛ إن استمرت المشكلة تحقق من اتصال الإنترنت |
| بعد الطرد، لا أستطيع الدخول من الجهاز الأول مجدداً | هذا طبيعي تماماً — فقط سجّل الدخول مجدداً بشكل عادي، وستصبح أنت الجلسة النشطة الجديدة |

---

**« معايير الجودة في التعليم الصيدلي.. واحترافية الممارسة الإكلينيكية »**
