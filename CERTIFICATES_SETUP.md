# تفعيل نظام شهادات إتمام الدورات — دليل التركيب

## 📦 الملفات الجديدة/المُحدَّثة في هذه الحزمة

| الملف | الحالة |
|---|---|
| `firestore.rules` | 🔄 محدّث — إضافة مجموعة `issuedCertificates` قابلة للقراءة العامة (بدون تسجيل دخول) للتحقق من الشهادات |
| `public/js/courses-data.js` | 🔄 محدّث — إضافة حقل `certificateCaseId` لكل دورة (يربطها بالحالة السريرية المطلوبة) |
| `public/js/auth.js` | 🔄 محدّث — إضافة حقل `fullName` عند إنشاء الحساب + دالة `saveFullName()` |
| `public/js/sync.js` | 🔄 محدّث — إضافة `listenToProfileExtra()`, `markChapterComplete()`, `listenToMyClinicalCases()` |
| `public/js/certificates.js` | 🆕 جديد — منطق التحقق من الأهلية وتوليد ملف PDF |
| `public/course-player.html` | 🔄 محدّث — زر "✅ أنهيت هذا الفصل" + علامات ✅ على الفصول المكتملة |
| `public/courses.html` | 🔄 محدّث — زر "🎓 الشهادة"، نافذة الاسم الكامل، قالب الشهادة المخفي |
| `public/verify.html` | 🆕 جديد — صفحة تحقق عامة من صحة أي شهادة برقمها (بدون تسجيل دخول) |
| `CERTIFICATES_SETUP.md` | 🆕 هذا الدليل |

---

## 🧠 كيف يعمل النظام؟

### 1. متطلبات الحصول على الشهادة
- ✅ الضغط على زر **"أنهيت هذا الفصل"** أسفل فيديو **كل فصل** في الدورة
- ✅ (فقط للدورات المرتبطة بحالة سريرية، مثل دورة OTC) اجتياز الحالة السريرية بدرجة **70% أو أعلى**

### 2. عند استيفاء الشروط
يظهر تلقائياً زر **"🎓 الشهادة"** بجانب بطاقة الدورة في `courses.html`.

### 3. عند الضغط على "🎓 الشهادة"
- إن لم يكن الطالب قد أدخل **اسمه الكامل** من قبل، تظهر نافذة لإدخاله أولاً (مرة واحدة فقط، يُحفظ للأبد).
- يُبنى قالب شهادة HTML/CSS مخفي في الصفحة، يُملأ ببيانات الطالب والدورة.
- تلتقط مكتبة **html2canvas** هذا القالب كصورة عالية الجودة (لضمان عرض النص العربي بشكل صحيح تماماً).
- تحوّل مكتبة **jsPDF** هذه الصورة إلى ملف PDF يُنزَّل تلقائياً على جهاز الطالب.
- يُسجَّل رقم شهادة فريد (مثل `JPA-2026-A1B2C3D4`) في Firestore، في مكانين:
  - ضمن ملف الطالب الشخصي (`users/{uid}.certificates`)
  - في مجموعة عامة `issuedCertificates` قابلة للقراءة من أي شخص (بدون تسجيل دخول) للتحقق لاحقاً

### 4. صفحة التحقق العامة (`verify.html`)
أي جهة (صاحب عمل، جهة تدريب أخرى) يمكنها زيارة:
```
https://hassanzawia.github.io/jpa-academy/verify.html
```
وإدخال رقم الشهادة للتحقق الفوري من صحتها، دون الحاجة لأي حساب.

---

## ⚠️ خطوة حرجة: نشر قواعد Firestore الجديدة يدوياً

كما فعلنا في التحديثات السابقة، **يجب نشر `firestore.rules` الجديدة** عبر Firebase Console:

1. اذهب إلى https://console.firebase.google.com → مشروع `pharmacyacadimy`
2. **Firestore Database** → تبويب **"Rules"**
3. امسح كل المحتوى الحالي (`Ctrl+A` ثم Delete)
4. الصق محتوى ملف `firestore.rules` المرفق في هذه الحزمة بالكامل
5. اضغط **"Publish"**

> 🔒 بدون هذه الخطوة، ستحصل على خطأ صلاحيات عند محاولة حفظ الشهادة، وستفشل صفحة `verify.html` في العثور على أي شهادة.

---

## 🚀 خطوات النسخ والرفع

### الخطوة 1: انسخ كل الملفات إلى مشروعك (استبدال/إضافة)
```
firestore.rules                    (استبدال)
public/js/courses-data.js          (استبدال)
public/js/auth.js                  (استبدال)
public/js/sync.js                  (استبدال)
public/js/certificates.js          (جديد)
public/course-player.html          (استبدال)
public/courses.html                (استبدال)
public/verify.html                 (جديد)
```

### الخطوة 2: انشر قواعد Firestore يدوياً (الخطوة أعلاه)

### الخطوة 3: ارفع التحديث
```powershell
cd "C:\Users\HP\OneDrive - Hatif Libya\Desktop\pharmacademy"
git add .
git commit -m "Add course completion certificates system"
git push
```

---

## 🧪 خطوات الاختبار الكامل

### الخطوة 1: امسح ذاكرة التخزين المؤقت
F12 → Application → Storage → Clear site data (لتجنب أي مشاكل كاش سابقة)

### الخطوة 2: سجّل الدخول واختبر دورة OTC (المرتبطة بحالة سريرية)
1. سجّل الدخول بحساب `dr_sarah` (مشتركة بالفعل في دورة OTC)
2. اذهب لصفحة الدورة، وشغّل كل فصل واضغط **"✅ أنهيت هذا الفصل"** لكل فصل (3 فصول)
3. أجب على الحالة السريرية بالإجابة **الصحيحة** (أسبرين / NSAIDs) لضمان درجة 100%
4. ارجع لصفحة `courses.html` — يجب أن يظهر الآن زر **"🎓 الشهادة"** بجانب الدورة

### الخطوة 3: حمّل الشهادة
1. اضغط **"🎓 الشهادة"**
2. أدخل اسمك الكامل عند الطلب (أول مرة فقط)
3. يجب أن يُنزَّل ملف PDF باسم `otc-pharmacy-practice-certificate.pdf`
4. افتح الملف وتحقق من:
   - ظهور اسمك بشكل صحيح
   - اسم الدورة والمدرّس والتاريخ
   - رقم الشهادة أسفل المستند

### الخطوة 4: تحقق من الشهادة عبر verify.html
1. انسخ رقم الشهادة الظاهر أسفل ملف PDF
2. اذهب إلى `https://hassanzawia.github.io/jpa-academy/verify.html`
3. الصق الرقم واضغط **"تحقق الآن"**
4. يجب أن تظهر بطاقة خضراء **"✅ شهادة صحيحة وموثّقة"** مع كل التفاصيل

### الخطوة 5: (اختباري) تحقق من رفض دورة غير مكتملة
جرّب فتح دورة "المصطلحات الطبية" (بدون إنهاء فصولها) — يجب **ألا** يظهر زر "🎓 الشهادة" إطلاقاً.

---

## 🛠️ حل المشاكل الشائعة

| المشكلة | السبب | الحل |
|---|---|---|
| زر "الشهادة" لا يظهر رغم إنهاء كل الفصول | لم تُنشر قواعد Firestore بعد، أو لم يُجتَز الاختبار السريري بدرجة كافية | تحقق من نشر القواعد، وتأكد من الإجابة الصحيحة في الحالة السريرية |
| `Missing or insufficient permissions` عند تحميل الشهادة | قواعد `issuedCertificates` لم تُنشر | راجع خطوة "نشر قواعد Firestore" أعلاه |
| الملف PDF يُنزَّل فارغاً أو مشوّهاً | فشل تحميل مكتبة html2canvas/jsPDF (اتصال إنترنت بطيء) | أعد المحاولة، تأكد من اتصال الإنترنت |
| صفحة verify.html تقول "غير موجودة" لشهادة صحيحة | خطأ إملائي بسيط عند نسخ رقم الشهادة | انسخ الرقم بالكامل ودقة من ملف PDF مباشرة |
| زر "أنهيت هذا الفصل" لا يستجيب | مشكلة اتصال مؤقتة بـ Firestore | أعد تحميل الصفحة وحاول مجدداً |

---

## 💡 ملاحظة حول الحالة السريرية المشتركة

حالياً، نفس السؤال السريري ("حالة Warfarin") يظهر في صفحة **كل** دورة (تبسيط من المرحلة الأولى للمشروع). هذا يعني أن حل هذا السؤال مرة واحدة (بغض النظر عن أي دورة كنت تتصفحها) يُسجَّل بنفس معرّف `case-otc-warfarin`، وبالتالي يُحتسب فقط لصالح الدورات التي حدّدت هذا المعرّف تحديداً في `certificateCaseId` (حالياً: دورة OTC فقط). يمكن لاحقاً توسيع هذا بإضافة حالات سريرية مختلفة مخصصة لكل دورة.

---

**« معايير الجودة في التعليم الصيدلي.. واحترافية الممارسة الإكلينيكية »**
