# إصلاح: عدم عمل تقييد الجهاز الواحد (Race Condition في تسجيل الدخول)

## 🐛 وصف المشكلة
بعد تفعيل ميزة "تقييد الدخول لجهاز واحد"، لاحظت أن تسجيل الدخول بنفس الحساب من جهازين مختلفين **لم يطرد** الجهاز الأول كما هو متوقع.

## 🔍 السبب الجذري (Race Condition حقيقي في Firebase Auth)

في `login.html` القديم كان يوجد:
```javascript
auth.onAuthStateChanged((user) => {
  if (user) window.location.href = "courses.html";
});
```

هذا "المستمع" (listener) يُستدعى من طرف Firebase SDK بمجرد تغيّر حالة تسجيل الدخول داخلياً — وهذا **قد يحدث قبل** اكتمال تنفيذ سلسلة `.then()` الخاصة بـ `loginWithEmail()` (التي تستدعي `ensureUserProfile()` ثم `startNewSession()`).

**النتيجة الفعلية:** أحياناً يفوز هذا المستمع في "السباق" ويُعيد التوجيه لصفحة `courses.html` **قبل** أن يكتب `startNewSession()` معرّف الجلسة الجديد في Firestore. وبالتالي:
- يبقى `activeSessionId` في Firestore كما هو من آخر دخول ناجح فعلياً
- لا يحدث أي "تعارض" حين يدخل جهاز آخر لاحقاً بنفس الحساب، لأن معرّف الجلسة القديم لم يتغيّر أصلاً في هذه الحالة

هذا يفسّر بدقة ما لاحظته: "يعمل مباشرة بعد اختيار المستخدم دون الضغط على دخول" — كنت تشاهد فعلياً نتيجة المستمع العام يفوز بالسباق أحياناً، وليس مسار تسجيل الدخول الكامل الصحيح.

---

## ✅ الإصلاح المُطبّق

أضفت **علامة حماية (`loginInProgress`)** في `login.html`:

1. عند الضغط على أي زر دخول (يدوي أو حساب تجريبي)، تُرفَع العلامة `loginInProgress = true` **فوراً وبشكل متزامن (synchronous)** قبل استدعاء `loginWithEmail()` غير المتزامنة.
2. المستمع العام (`auth.onAuthStateChanged`) أصبح يتحقق من هذه العلامة، ولا يُعيد التوجيه إلا إذا كانت **غير مرفوعة** (أي أن المستخدم كان مسجّلاً دخوله بالفعل من قبل، وليس في خضم عملية دخول جديدة).
3. التوجيه الفعلي بعد نجاح الدخول يحدث فقط من داخل `loginWithEmail()` نفسها، بعد اكتمال `startNewSession()` بنجاح تام.

هذا يضمن **ترتيباً حتمياً (deterministic)**: `startNewSession()` يُكمل الكتابة في Firestore **قبل** أي توجيه لصفحة أخرى، مهما كانت سرعة استجابة Firebase.

---

## 📦 الملف المُحدَّث

| الملف | التغيير |
|---|---|
| `public/login.html` | 🔄 إعادة كتابة منطق الدخول بالكامل لإصلاح Race Condition |

---

## 🚀 خطوات النسخ والرفع

```powershell
cd "C:\Users\HP\OneDrive - Hatif Libya\Desktop\pharmacademy"
```
انسخ `login.html` الجديد من هذه الحزمة إلى `public\login.html` (استبدال).

```powershell
git add .
git commit -m "Fix race condition preventing single-device session enforcement"
git push
```

---

## 🧪 خطوات إعادة الاختبار (من الصفر تماماً)

### الخطوة 1: امسح كل شيء أولاً في كلا المتصفحين
F12 → Application → Storage → **Clear site data** (في كل نافذة/متصفح ستستخدمه للاختبار)

### الخطوة 2: أغلق كل التبويبات المفتوحة على الموقع تماماً

### الخطوة 3: اختبار جديد من الصفر
1. **المتصفح/النافذة الأولى**: افتح `login.html`، اضغط زر حساب `nour_intern`، انتظر حتى تصل فعلياً لصفحة `courses.html` وتظهر بطاقات الدورات.
2. انتظر 5 ثوانٍ كاملة (تأكيد أن كل شيء استقر).
3. **المتصفح/النافذة الثانية** (مختلفة تماماً، أو Incognito): افتح `login.html`، اضغط زر **نفس** حساب `nour_intern`.
4. **ارجع فوراً للنافذة الأولى** دون تحديث الصفحة يدوياً.

### 👀 النتيجة المتوقعة الآن
خلال ثوانٍ قليلة، يجب أن يظهر في **النافذة الأولى** تلقائياً:
```
⚠️ تم تسجيل الدخول إلى هذا الحساب من جهاز آخر.
سيتم تسجيل خروجك من هذا الجهاز الآن.
```
ثم إعادة توجيه تلقائية إلى `login.html`.

---

## 🔍 التحقق التقني الإضافي (اختياري، لمن يريد التأكد الكامل)

بعد الخطوة 3 (تسجيل الدخول من النافذة الثانية)، افتح Firebase Console → Firestore Database → Data → `users` → مستند `nour_intern`، وتحقق من وجود حقل `activeSessionId` **بقيمة جديدة تماماً** مختلفة عن أي قيمة سابقة، مع `activeSessionAt` يعكس الوقت الحالي بدقة.

---

**« معايير الجودة في التعليم الصيدلي.. واحترافية الممارسة الإكلينيكية »**
